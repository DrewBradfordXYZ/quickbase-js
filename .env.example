# QuickBase Integration Test Configuration
#
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env
#
# These credentials are used for integration tests only.
# Unit tests use mocked fixtures and don't require credentials.
#
# The tests will create an ephemeral app for each run
# and automatically clean it up when tests complete.

# Your QuickBase realm (the subdomain before .quickbase.com)
QB_REALM=mycompany

# Your QuickBase user token
# Generate at: https://mycompany.quickbase.com/db/main?a=UserTokens
# Token must have permission to create/delete apps
QB_USER_TOKEN=xxxx_xxx_x_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
